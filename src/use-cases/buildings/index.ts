import { createBuilding } from "./create-building";
import { fetchProfileBuildings } from "./fetch-profile-building";

export const BuildingsUseCases = {
  createBuilding,
  fetchProfileBuildings,
};
